use strict;
use warnings;
use FindBin;

use Test::More skip_all => "Tests disabled"

# use lib ("$FindBin::Bin/../../lib");
# use Test::More;
# 
# if ($ENV{'ESCROW_LIVE'}) {
#     print "woot";
#     plan tests => 3;
# } else {
#     plan skip_all => "Enable live tests by setting ESCROW_LIVE=1"
# }
# use Catalyst::Test 'Escrow::Service';
# use Data::Dumper;
# 
# ok( get('/') =~ /^\^Data::Dumper/, "Root returns Data::Serializer" );
# ok( get('/boardwalk-legion') =~ /^\^Data::Dumper/,
#     "Things return Data::Serializer" );
# ok(
#     get('/boardwalk-legion/BUDDHA') =~ /^\^Data::Dumper/,
#     "Thing specific keys return Data::Serializer"
# );

